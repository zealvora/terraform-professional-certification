
```sh
apt install python3-pip
python3 -m venv .venv
apt install python3.12-venv
python3 -m venv .venv
source .venv/bin/activate
pip install checkov
```
```sh
apt-get update 

apt-get install python3-pip

apt install python3.12-venv (aws)

python3 -m venv .venv

source .venv/bin/activate

pip install checkov
```
